/**
 *
 * @游戏配置
 *
 */
var GameConfig = (function () {
    function GameConfig() {
    }
    var d = __define,c=GameConfig,p=c.prototype;
    //初始化配置数据
    GameConfig.init = function (is_Test) {
    };
    GameConfig.gameName = 'ChaoGu';
    return GameConfig;
})();
egret.registerClass(GameConfig,'GameConfig');
